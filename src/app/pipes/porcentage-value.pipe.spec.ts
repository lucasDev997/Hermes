import { PorcentageValuePipe } from './porcentage-value.pipe';

describe('PorcentageValuePipe', () => {
  it('create an instance', () => {
    const pipe = new PorcentageValuePipe();
    expect(pipe).toBeTruthy();
  });
});
